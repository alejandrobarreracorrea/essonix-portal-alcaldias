# Plan 5 — Ley 617 (Indicador de Autofinanciación) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reproducir el indicador de Ley 617 de 2000 (autofinanciación de gastos de funcionamiento = GF Admón Central depurado / ICLD) con valores idénticos a la hoja "Ley 617" de la plantilla (tolerancia 0), validado con golden test contra Briceño.

**Architecture:** Función pura que calcula un objeto de indicador (no una tabla) a partir de los insumos ya parseados: `IngresoRawRow[]` (ICLD por fuente, recaudo) y `GastoRawRow[]` (compromisos = "Registros" por unidad ejecutora / rubro / fuente). Reusa SUMIF/VLOOKUP-equivalentes sobre datos que ya cuadran con la plantilla (los goldens de ingresos/gastos pasan), así que es totalmente verificable a tolerancia 0. Este indicador es también el "Indicador 1 (Autofinanciación)" de la hoja documental "Indicadores".

**Tech Stack:** React 18, TypeScript, Vite, SheetJS (`xlsx` 0.20.3), Tailwind, Vitest. (Planes 1–4 completos: Ingresos, Gastos, Fuentes y Usos, ingesta unificada.)

## Global Constraints

- **Fidelidad tolerancia 0:** cada valor numérico/booleano-texto idéntico al de la hoja "Ley 617" (comparación estricta).
- **Valor de compromisos = columna "Registros"** de gastos (`GastoRawRow.registros`), NO egresos ni presupuesto.
- **VLOOKUP = primera coincidencia:** las búsquedas por rubro/fuente devuelven el PRIMER registro que coincide (no suma). SUMIF = suma de todas las coincidencias en orden de filas.
- **IFERROR→0:** búsquedas sin coincidencia y divisiones por cero devuelven 0.
- **Orden de operaciones exacto** en `totalAdminDepurado` (resta secuencial H18-H19-H20-H21-H22-H23-H24-H25-H26-H28-H27, con H28 antes de H27) para paridad de punto flotante.
- **Criterios (cadenas completas código+descripción)** son los de un municipio categoría 4/5/6 (límite 0.8). Match exacto tras trim.
- **Motor puro.** Aritmética IEEE-754 sin redondeos. Identificadores English/camelCase, UI español. No tocar motores/golden previos.

## Estructura de archivos

```
src/
  engine/
    ley617.ts            # NUEVO
    ley617.test.ts       # NUEVO
    ley617.golden.test.ts # NUEVO
  types.ts               # MODIFICAR: IndicadorLey617
  ui/
    PanelLey617.tsx      # NUEVO
    PanelLey617.test.tsx # NUEVO
    Dashboard.tsx        # MODIFICAR: pestaña Ley 617
    App.tsx              # MODIFICAR: calcular y pasar indicadorLey617
scripts/
  extract-golden-ley617.mjs  # NUEVO
tests/
  golden/ley617.expected.json # NUEVO (generado)
```

### Reglas de cálculo (verificadas contra Briceño)

Criterios (constantes):
```
ICLD_PROPIO              = "1.2.1.0.00 - INGRESOS CORRIENTES DE LIBRE DESTINACION"
SGP_LIBRE_DEST          = "1.2.4.3.04 - SGP-PROPOSITO GENERAL-LIBRE DESTINACION MUNICIPIOS CATEGORIAS 4, 5 Y 6"
RUBRO_FUNCIONAMIENTO    = "2.1"
UNIDAD_CONCEJO          = "18.0 - ENTIDADES TERRITORIALES - CONCEJO"
UNIDAD_PERSONERIA       = "20.0 - ENTIDADES TERRITORIALES - PERSONERIA"
FUENTE_SOBRETASA_AMB    = "1.2.3.1.01 - SOBRETASA - PARTICIPACION AMBIENTAL - CORPORACIONES AUTONOMAS REGIONALES"
FUENTE_SOBRETASA_BOMB   = "1.2.3.1.14 - SOBRETASA BOMBERIL"
RUBRO_SEGURIDAD_CONC    = "2.1.1.01.02.020.02"
RUBRO_TRANSPORTE_CONC   = "2.1.1.01.03.125"
RUBRO_CUOTAS_PARTES     = "2.1.3.07.02.002"
FUENTE_RETIROS_FONPET   = "1.3.1.1.10 - RETIROS FONPET"
FUENTE_COLJUEGOS        = "1.2.3.2.28 - DERECHOS POR LA EXPLOTACION JUEGOS DE SUERTE Y AZAR"
LIMITE_617              = 0.8
```

Helpers sobre los insumos:
- `sumIngresoPorFuente(f)` = Σ `IngresoRawRow.totalIngresos` donde `ccpet05 == f`.
- `sumGastoPorUnidad(u)` = Σ `GastoRawRow.registros` donde `unidadEjec == u`.
- `sumGastoPorFuente(f)` = Σ `GastoRawRow.registros` donde `fuentes == f`.
- `lookupRubro(cod)` = `registros` del PRIMER `GastoRawRow` con `rubro == cod`, si no 0.
- `lookupFuente(f)` = `registros` del PRIMER `GastoRawRow` con `fuentes == f`, si no 0.

Cálculo:
```
icldPropio (H6)       = sumIngresoPorFuente(ICLD_PROPIO)
sgpLibreDest (H7)     = sumIngresoPorFuente(SGP_LIBRE_DEST)
totalIcld (H8/H33)    = icldPropio + sgpLibreDest
concejo (G12)         = sumGastoPorUnidad(UNIDAD_CONCEJO)
personeria (G13)      = sumGastoPorUnidad(UNIDAD_PERSONERIA)
funcAdminCentral(G11) = lookupRubro(RUBRO_FUNCIONAMIENTO) - concejo - personeria
gfTotal (G14)         = funcAdminCentral + concejo + personeria
baseAdmin (H18)       = funcAdminCentral
dedSobretasaAmb (H19) = sumGastoPorFuente(FUENTE_SOBRETASA_AMB)
dedSobretasaBomb(H20) = sumGastoPorFuente(FUENTE_SOBRETASA_BOMB)
dedSeguridadConc(H21) = lookupRubro(RUBRO_SEGURIDAD_CONC)
dedSeguroVida (H22)   = 0   (sin clave en la plantilla)
dedPolizaSalud (H23)  = 0   (sin clave)
dedTransporteConc(H24)= lookupRubro(RUBRO_TRANSPORTE_CONC)
dedDeficit (H25)      = 0   (sin clave)
dedCuotasPartes (H26) = lookupRubro(RUBRO_CUOTAS_PARTES)
dedMesadas (H27)      = lookupFuente(FUENTE_RETIROS_FONPET)
dedColjuegos (H28)    = lookupFuente(FUENTE_COLJUEGOS)
totalAdminDepurado(H32) = baseAdmin - dedSobretasaAmb - dedSobretasaBomb - dedSeguridadConc - dedSeguroVida - dedPolizaSalud - dedTransporteConc - dedDeficit - dedCuotasPartes - dedColjuegos - dedMesadas
                          // (orden EXACTO: H18-H19-H20-H21-H22-H23-H24-H25-H26-H28-H27)
pctGfIcld (H34)       = totalIcld !== 0 ? totalAdminDepurado / totalIcld : 0
limite (H35)          = 0.8
diferencial (H36)     = limite - pctGfIcld
cumplimiento (H37)    = pctGfIcld < limite ? "Cumple" : "No Cumple"
```

Valores golden de control: icldPropio 5838830994.180001; sgpLibreDest 1083338192; totalIcld 6922169186.180001; funcAdminCentral 2785887779.98; concejo 193257794; personeria 85695471; totalAdminDepurado 2663634367.98; pctGfIcld 0.3847976402105142; diferencial 0.41520235978948583; cumplimiento "Cumple".

---

### Task 1: Motor `ley617`

**Files:**
- Modify: `src/types.ts`
- Create: `src/engine/ley617.ts`, `src/engine/ley617.test.ts`

**Interfaces:**
- Consumes: `IngresoRawRow`, `GastoRawRow`, `IndicadorLey617`.
- Produces: `function ley617(ingresoRows: IngresoRawRow[], gastoRows: GastoRawRow[]): IndicadorLey617`

- [ ] **Step 1: Añadir `IndicadorLey617` a `types.ts`**

```ts
export type IndicadorLey617 = {
  icldPropio: number
  sgpLibreDest: number
  totalIcld: number
  funcAdminCentral: number
  concejo: number
  personeria: number
  gfTotal: number
  baseAdmin: number
  dedSobretasaAmb: number
  dedSobretasaBomb: number
  dedSeguridadConc: number
  dedSeguroVida: number
  dedPolizaSalud: number
  dedTransporteConc: number
  dedDeficit: number
  dedCuotasPartes: number
  dedMesadas: number
  dedColjuegos: number
  totalAdminDepurado: number
  pctGfIcld: number
  limite: number
  diferencial: number
  cumplimiento: string
}
```

- [ ] **Step 2: Escribir el test del motor (falla)**

Create `src/engine/ley617.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { ley617 } from './ley617'
import type { IngresoRawRow, GastoRawRow } from '../types'

const ing = (ccpet05: string, totalIngresos: number): IngresoRawRow => ({
  codigoRubro: 'x', descripcion: '', ccpet02: '', ccpet05, ccpet83: '',
  pptoInicial: 0, adicAnteriores: 0, adicPeriodo: 0, reducAnteriores: 0, reducPeriodo: 0,
  pptoFinal: 0, totalIngresos,
})
const gas = (p: Partial<GastoRawRow>): GastoRawRow => ({
  rubro: 'x', descripcion: '', cpc: '', unidadEjec: '', programatico: '', fuentes: '', cpi: '', atributo: '',
  pptoInicial: 0, pptoFinal: 0, disponibilidades: 0, saldoDisponible: 0, registros: 0,
  saldoDisponibilidades: 0, ordenPago: 0, saldoRegistro: 0, egresos: 0, egresosPapeles: 0,
  saldoOrdenesPago: 0, ...p,
})

const ICLD = '1.2.1.0.00 - INGRESOS CORRIENTES DE LIBRE DESTINACION'
const SGP = '1.2.4.3.04 - SGP-PROPOSITO GENERAL-LIBRE DESTINACION MUNICIPIOS CATEGORIAS 4, 5 Y 6'

describe('ley617', () => {
  it('calcula el indicador y el cumplimiento', () => {
    const ingresos = [ing(ICLD, 1000), ing(SGP, 200), ing('OTRA', 9999)]
    const gastos = [
      gas({ rubro: '2.1', registros: 500 }),               // funcionamiento total (lookup)
      gas({ unidadEjec: '18.0 - ENTIDADES TERRITORIALES - CONCEJO', registros: 100 }),
      gas({ unidadEjec: '20.0 - ENTIDADES TERRITORIALES - PERSONERIA', registros: 50 }),
      gas({ fuentes: '1.2.3.1.14 - SOBRETASA BOMBERIL', registros: 30 }),
    ]
    const r = ley617(ingresos, gastos)
    expect(r.totalIcld).toBe(1200)
    expect(r.concejo).toBe(100)
    expect(r.personeria).toBe(50)
    expect(r.funcAdminCentral).toBe(350) // 500 - 100 - 50
    expect(r.dedSobretasaBomb).toBe(30)
    expect(r.totalAdminDepurado).toBe(320) // 350 - 30 (resto deducciones 0)
    expect(r.pctGfIcld).toBe(320 / 1200)
    expect(r.diferencial).toBe(0.8 - 320 / 1200)
    expect(r.cumplimiento).toBe('Cumple')
  })

  it('lookup usa la PRIMERA coincidencia, no suma', () => {
    const gastos = [gas({ rubro: '2.1', registros: 500 }), gas({ rubro: '2.1', registros: 999 })]
    expect(ley617([], gastos).funcAdminCentral).toBe(500)
  })

  it('% es 0 si el ICLD es 0 (IFERROR) y marca No Cumple si supera el límite', () => {
    const sinIcld = ley617([], [gas({ rubro: '2.1', registros: 500 })])
    expect(sinIcld.pctGfIcld).toBe(0)
    expect(sinIcld.cumplimiento).toBe('Cumple') // 0 < 0.8
    const supera = ley617([ing(ICLD, 100)], [gas({ rubro: '2.1', registros: 90 })])
    expect(supera.pctGfIcld).toBe(0.9)
    expect(supera.cumplimiento).toBe('No Cumple')
  })
})
```

- [ ] **Step 3: Correr (verificar que falla)**

Run: `npm test -- src/engine/ley617.test.ts`
Expected: FAIL (`ley617` no existe).

- [ ] **Step 4: Implementar el motor**

Create `src/engine/ley617.ts`:
```ts
import type { GastoRawRow, IndicadorLey617, IngresoRawRow } from '../types'

const ICLD_PROPIO = '1.2.1.0.00 - INGRESOS CORRIENTES DE LIBRE DESTINACION'
const SGP_LIBRE_DEST = '1.2.4.3.04 - SGP-PROPOSITO GENERAL-LIBRE DESTINACION MUNICIPIOS CATEGORIAS 4, 5 Y 6'
const RUBRO_FUNCIONAMIENTO = '2.1'
const UNIDAD_CONCEJO = '18.0 - ENTIDADES TERRITORIALES - CONCEJO'
const UNIDAD_PERSONERIA = '20.0 - ENTIDADES TERRITORIALES - PERSONERIA'
const FUENTE_SOBRETASA_AMB = '1.2.3.1.01 - SOBRETASA - PARTICIPACION AMBIENTAL - CORPORACIONES AUTONOMAS REGIONALES'
const FUENTE_SOBRETASA_BOMB = '1.2.3.1.14 - SOBRETASA BOMBERIL'
const RUBRO_SEGURIDAD_CONC = '2.1.1.01.02.020.02'
const RUBRO_TRANSPORTE_CONC = '2.1.1.01.03.125'
const RUBRO_CUOTAS_PARTES = '2.1.3.07.02.002'
const FUENTE_RETIROS_FONPET = '1.3.1.1.10 - RETIROS FONPET'
const FUENTE_COLJUEGOS = '1.2.3.2.28 - DERECHOS POR LA EXPLOTACION JUEGOS DE SUERTE Y AZAR'
const LIMITE_617 = 0.8

export function ley617(ingresoRows: IngresoRawRow[], gastoRows: GastoRawRow[]): IndicadorLey617 {
  const sumIngresoPorFuente = (f: string) =>
    ingresoRows.reduce((a, r) => (r.ccpet05.trim() === f ? a + (r.totalIngresos || 0) : a), 0)
  const sumGastoPorUnidad = (u: string) =>
    gastoRows.reduce((a, r) => (r.unidadEjec.trim() === u ? a + (r.registros || 0) : a), 0)
  const sumGastoPorFuente = (f: string) =>
    gastoRows.reduce((a, r) => (r.fuentes.trim() === f ? a + (r.registros || 0) : a), 0)
  const lookupRubro = (cod: string) => {
    const row = gastoRows.find((r) => r.rubro.trim() === cod)
    return row ? row.registros || 0 : 0
  }
  const lookupFuente = (f: string) => {
    const row = gastoRows.find((r) => r.fuentes.trim() === f)
    return row ? row.registros || 0 : 0
  }

  const icldPropio = sumIngresoPorFuente(ICLD_PROPIO)
  const sgpLibreDest = sumIngresoPorFuente(SGP_LIBRE_DEST)
  const totalIcld = icldPropio + sgpLibreDest

  const concejo = sumGastoPorUnidad(UNIDAD_CONCEJO)
  const personeria = sumGastoPorUnidad(UNIDAD_PERSONERIA)
  const funcAdminCentral = lookupRubro(RUBRO_FUNCIONAMIENTO) - concejo - personeria
  const gfTotal = funcAdminCentral + concejo + personeria

  const baseAdmin = funcAdminCentral
  const dedSobretasaAmb = sumGastoPorFuente(FUENTE_SOBRETASA_AMB)
  const dedSobretasaBomb = sumGastoPorFuente(FUENTE_SOBRETASA_BOMB)
  const dedSeguridadConc = lookupRubro(RUBRO_SEGURIDAD_CONC)
  const dedSeguroVida = 0
  const dedPolizaSalud = 0
  const dedTransporteConc = lookupRubro(RUBRO_TRANSPORTE_CONC)
  const dedDeficit = 0
  const dedCuotasPartes = lookupRubro(RUBRO_CUOTAS_PARTES)
  const dedMesadas = lookupFuente(FUENTE_RETIROS_FONPET)
  const dedColjuegos = lookupFuente(FUENTE_COLJUEGOS)

  // Orden EXACTO de la plantilla: H18-H19-H20-H21-H22-H23-H24-H25-H26-H28-H27
  const totalAdminDepurado =
    baseAdmin - dedSobretasaAmb - dedSobretasaBomb - dedSeguridadConc - dedSeguroVida -
    dedPolizaSalud - dedTransporteConc - dedDeficit - dedCuotasPartes - dedColjuegos - dedMesadas

  const pctGfIcld = totalIcld !== 0 ? totalAdminDepurado / totalIcld : 0
  const diferencial = LIMITE_617 - pctGfIcld
  const cumplimiento = pctGfIcld < LIMITE_617 ? 'Cumple' : 'No Cumple'

  return {
    icldPropio, sgpLibreDest, totalIcld,
    funcAdminCentral, concejo, personeria, gfTotal, baseAdmin,
    dedSobretasaAmb, dedSobretasaBomb, dedSeguridadConc, dedSeguroVida, dedPolizaSalud,
    dedTransporteConc, dedDeficit, dedCuotasPartes, dedMesadas, dedColjuegos,
    totalAdminDepurado, pctGfIcld, limite: LIMITE_617, diferencial, cumplimiento,
  }
}
```

- [ ] **Step 5: Correr (verificar que pasa)**

Run: `npm test -- src/engine/ley617.test.ts`
Expected: PASS (3 casos).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: motor ley617 (indicador de autofinanciación GF/ICLD)"
```

---

### Task 2: Golden test de Ley 617 (Briceño)

**Files:**
- Create: `scripts/extract-golden-ley617.mjs`, `tests/golden/ley617.expected.json` (generado)
- Test: `src/engine/ley617.golden.test.ts`

**Interfaces:**
- Consumes: `normalizeIngresoRows`/`normalizeGastoRows`/`sheetToMatrix`, `ley617`, fixtures de ingresos/gastos.
- Produces: prueba `insumos Briceño → motor === valores de la hoja "Ley 617"`, tolerancia 0.

- [ ] **Step 1: Escribir el script de extracción**

Create `scripts/extract-golden-ley617.mjs`:
```js
import { createRequire } from 'node:module'
import { writeFileSync } from 'node:fs'
const require = createRequire(import.meta.url)
const XLSX = require('xlsx')

const TEMPLATE = process.env.TEMPLATE ??
  '/Users/alejandro/Documents/DocumentosProyectoGobierno/1. Análisis Ejecución de Ingresos y Gastos Fuentes y Usos V2025 Corte 31-05-2026 Briceño.xlsx'

const wb = XLSX.readFile(TEMPLATE)
const ws = wb.Sheets['Ley 617']
if (!ws) { console.error('No se encontró la hoja "Ley 617"'); process.exit(1) }
const cell = (addr) => { const c = ws[addr]; return c == null ? null : c.v }

const out = {
  icldPropio: cell('H6'),
  sgpLibreDest: cell('H7'),
  totalIcld: cell('H8'),
  funcAdminCentral: cell('G11'),
  concejo: cell('G12'),
  personeria: cell('G13'),
  gfTotal: cell('G14'),
  baseAdmin: cell('H18'),
  dedSobretasaAmb: cell('H19'),
  dedSobretasaBomb: cell('H20'),
  dedSeguridadConc: cell('H21'),
  dedSeguroVida: cell('H22') ?? 0,
  dedPolizaSalud: cell('H23') ?? 0,
  dedTransporteConc: cell('H24'),
  dedDeficit: cell('H25') ?? 0,
  dedCuotasPartes: cell('H26'),
  dedMesadas: cell('H27'),
  dedColjuegos: cell('H28'),
  totalAdminDepurado: cell('H32'),
  pctGfIcld: cell('H34'),
  limite: cell('H35'),
  diferencial: cell('H36'),
  cumplimiento: cell('H37'),
}
writeFileSync('tests/golden/ley617.expected.json', JSON.stringify(out, null, 2))
console.log('wrote tests/golden/ley617.expected.json:', JSON.stringify(out))
```

- [ ] **Step 2: Generar el JSON esperado**

Run: `node scripts/extract-golden-ley617.mjs`
Expected: crea el archivo; `pctGfIcld` ≈ 0.3847976402105142, `cumplimiento` "Cumple", `totalIcld` 6922169186.180001.

- [ ] **Step 3: Escribir el golden test**

Create `src/engine/ley617.golden.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import * as XLSX from 'xlsx'
import { readFileSync } from 'node:fs'
import { normalizeIngresoRows } from '../parsers/ingresos'
import { normalizeGastoRows } from '../parsers/gastos'
import { sheetToMatrix } from '../parsers/sheet'
import { ley617 } from './ley617'

describe('fidelidad Ley 617 (Briceño) — tolerancia 0', () => {
  it('la salida del motor coincide exactamente con la hoja "Ley 617"', () => {
    const wbI = XLSX.read(readFileSync('tests/fixtures/ingresos.briceno.xlsx'))
    const wbG = XLSX.read(readFileSync('tests/fixtures/gastos.briceno.xlsx'))
    const ingresoRows = normalizeIngresoRows(sheetToMatrix(wbI.Sheets[wbI.SheetNames[0]]))
    const gastoRows = normalizeGastoRows(sheetToMatrix(wbG.Sheets[wbG.SheetNames[0]]))

    const got = ley617(ingresoRows, gastoRows)
    const want = JSON.parse(readFileSync('tests/golden/ley617.expected.json', 'utf8'))

    expect(got).toEqual(want)
  })
})
```

- [ ] **Step 4: Correr el golden test (verificar que pasa)**

Run: `npm test -- src/engine/ley617.golden.test.ts`
Expected: PASS — todos los campos coinciden exactamente.

Si falla: comparar campo a campo. Causa probable: un criterio (cadena de fuente/rubro/unidad) mal transcrito → ese SUMIF/lookup da 0. NO relajar la tolerancia; corregir la cadena leyendo la celda exacta de la plantilla (A6/B6/C6/D6/E6/G19.. en la hoja "Ley 617"). Si solo difieren decimales, revisar el orden de la resta de `totalAdminDepurado`.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "test: golden de fidelidad de Ley 617 (Briceño, tolerancia 0)"
```

---

### Task 3: Panel y pestaña de Ley 617

**Files:**
- Create: `src/ui/PanelLey617.tsx`, `src/ui/PanelLey617.test.tsx`
- Modify: `src/ui/Dashboard.tsx`, `src/App.tsx`

**Interfaces:**
- Consumes: `IndicadorLey617`, `ley617`, `IngresoRawRow`/`GastoRawRow`.
- Produces: `PanelLey617` y una pestaña "Ley 617" que aparece cuando hay ingresos Y gastos.

- [ ] **Step 1: Escribir el test del panel (falla)**

Create `src/ui/PanelLey617.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { PanelLey617 } from './PanelLey617'
import type { IndicadorLey617 } from '../types'

const ind: IndicadorLey617 = {
  icldPropio: 5838830994.18, sgpLibreDest: 1083338192, totalIcld: 6922169186.18,
  funcAdminCentral: 2785887779.98, concejo: 193257794, personeria: 85695471, gfTotal: 3064841044.98,
  baseAdmin: 2785887779.98, dedSobretasaAmb: 30636311, dedSobretasaBomb: 48759212,
  dedSeguridadConc: 0, dedSeguroVida: 0, dedPolizaSalud: 0, dedTransporteConc: 15566000,
  dedDeficit: 0, dedCuotasPartes: 2291889, dedMesadas: 0, dedColjuegos: 25000000,
  totalAdminDepurado: 2663634367.98, pctGfIcld: 0.3847976402105142, limite: 0.8,
  diferencial: 0.41520235978948583, cumplimiento: 'Cumple',
}

describe('PanelLey617', () => {
  it('muestra el % del indicador y el cumplimiento', () => {
    render(<PanelLey617 indicador={ind} />)
    expect(screen.getByText('Cumple')).toBeInTheDocument()
    expect(screen.getByText(/38,48 %|38.48 %/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Correr (verificar que falla)**

Run: `npm test -- src/ui/PanelLey617.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Implementar `PanelLey617`**

Create `src/ui/PanelLey617.tsx`:
```tsx
import type { IndicadorLey617 } from '../types'

const fmtNum = (n: number) => n.toLocaleString('es-CO', { maximumFractionDigits: 2 })
const fmtPct = (n: number) => (n * 100).toLocaleString('es-CO', { maximumFractionDigits: 2 }) + ' %'

const FILAS: { label: string; key: keyof IndicadorLey617 }[] = [
  { label: 'ICLD recurso propio', key: 'icldPropio' },
  { label: 'SGP libre destinación', key: 'sgpLibreDest' },
  { label: 'Total ICLD', key: 'totalIcld' },
  { label: 'Funcionamiento Admón. Central', key: 'funcAdminCentral' },
  { label: '(−) Sobretasa ambiental', key: 'dedSobretasaAmb' },
  { label: '(−) Sobretasa bomberil', key: 'dedSobretasaBomb' },
  { label: '(−) Transporte concejales', key: 'dedTransporteConc' },
  { label: '(−) Cuotas partes', key: 'dedCuotasPartes' },
  { label: '(−) Coljuegos', key: 'dedColjuegos' },
  { label: 'Total Admón. Central (GF neto)', key: 'totalAdminDepurado' },
]

export function PanelLey617({ indicador }: { indicador: IndicadorLey617 }) {
  const cumple = indicador.cumplimiento === 'Cumple'
  return (
    <section className="space-y-4">
      <div className="rounded-lg border p-4">
        <p className="text-sm text-slate-500">Indicador Ley 617 — Autofinanciación de gastos de funcionamiento (GF / ICLD)</p>
        <div className="mt-1 flex items-baseline gap-3">
          <span className="text-3xl font-bold">{fmtPct(indicador.pctGfIcld)}</span>
          <span className={`rounded px-2 py-0.5 text-sm font-medium ${cumple ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
            {indicador.cumplimiento}
          </span>
          <span className="text-sm text-slate-500">Límite {fmtPct(indicador.limite)}</span>
        </div>
      </div>
      <table className="min-w-full border-collapse text-sm">
        <tbody>
          {FILAS.map((f) => (
            <tr key={f.key} className="odd:bg-white even:bg-slate-50">
              <td className="border px-2 py-1">{f.label}</td>
              <td className="border px-2 py-1 text-right">{fmtNum(indicador[f.key] as number)}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </section>
  )
}
```

- [ ] **Step 4: Correr (verificar que pasa)**

Run: `npm test -- src/ui/PanelLey617.test.tsx`
Expected: PASS.

- [ ] **Step 5: Añadir la pestaña al Dashboard**

In `src/ui/Dashboard.tsx`:
- Import `import type { ..., IndicadorLey617 } from '../types'` y `import { PanelLey617 } from './PanelLey617'`.
- Añadir prop opcional `indicadorLey617?: IndicadorLey617`.
- Tras el área de Fuentes y Usos, añadir:
```tsx
  if (indicadorLey617) {
    areas.push({
      id: 'ley617',
      label: 'Ley 617',
      render: () => <PanelLey617 indicador={indicadorLey617} />,
    })
  }
```

- [ ] **Step 6: Calcular y pasar `indicadorLey617` en `App.tsx`**

In `src/App.tsx`:
- Import `import { ley617 } from './engine/ley617'`.
- Memo (requiere ingresos Y gastos):
```tsx
  const indicadorLey617 = useMemo(
    () => (ingresoRows.length > 0 && gastoRows.length > 0 ? ley617(ingresoRows, gastoRows) : undefined),
    [ingresoRows, gastoRows],
  )
```
- Pasar la prop: `<Dashboard ... indicadorLey617={indicadorLey617} />`.

- [ ] **Step 7: Verificar suite y build**

Run: `npm test`
Expected: PASS — toda la suite (incluye el golden de Ley 617 y los previos intactos).

Run: `npm run build`
Expected: build exitoso, sin errores de TypeScript.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: panel y pestaña de Ley 617 en el dashboard"
```

---

## Self-Review

**Cobertura:** indicador GF/ICLD con ICLD (2 fuentes) y GF Admón Central depurado (deducciones) → Task 1; golden tolerancia 0 contra la hoja "Ley 617" → Task 2; panel + pestaña → Task 3. Cubre también el "Indicador 1 (Autofinanciación)" de la hoja documental Indicadores. ✓

**Placeholders:** sin TBD/TODO; código completo.

**Consistencia de tipos:** `IndicadorLey617` en `types.ts`; el motor usa `IngresoRawRow.ccpet05/totalIngresos` y `GastoRawRow.unidadEjec/rubro/fuentes/registros` (campos existentes). El golden extrae las mismas celdas (H6..H37) que el motor reproduce. Dashboard recibe `indicadorLey617`; App lo calcula solo con ingresos+gastos. No se tocan motores/golden previos.
